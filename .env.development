# Dev-only env. Vite auto-loads this when `vite dev` runs.
# Relative URL → browser fetches are same-origin, intercepted by the
# server.proxy block in vite.config.ts and forwarded to the Go API at
# localhost:8000. CORS becomes irrelevant in dev.
PUBLIC_GO_API_URL=/api/v1
